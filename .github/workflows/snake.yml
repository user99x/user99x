name: 🐍 Generate Snake Animation

on:
  schedule:
    - cron: "0 */12 * * *"  # Exécute toutes les 12 heures
  workflow_dispatch:  # Permet de le lancer manuellement

jobs:
  build:
    name: Générer le serpent
    runs-on: ubuntu-latest

    steps:
      - name: Récupérer le dépôt
        uses: actions/checkout@v3

      - name: Générer l'animation du serpent
        uses: Platane/snk@master
        with:
          github_user_name: user99x  # Ton pseudo GitHub
          outputs: dist/github-contribution-grid-snake.svg
          snake_color: "blue"  # Couleur du serpent

      - name: Publier sur GitHub Pages
        uses: crazy-max/ghaction-github-pages@v2.1.3
        with:
          target_branch: output  # Sauvegarde les fichiers dans la branche "output"
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
